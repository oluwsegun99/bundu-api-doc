info:
  name: Get Invite A Peer
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/me/invite-a-peer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns the same invite-link summary as `GET /me/invite-links`.
