info:
  name: Get Invite Links
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/me/invite-links"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: ""
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
            "success": true,
            "data": {
              "total": 0,
              "active": 0,
              "pending": 0,
              "items": []
            }
          }
