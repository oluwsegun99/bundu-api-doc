info:
  name: Get Upgrade
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/me/upgrade"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: ""
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
            "success": true,
            "data": {
              "currentTier": 2,
              "nextTier": 3,
              "message": "Unlock higher contribution limits and deeper profile visibility."
            }
          }
