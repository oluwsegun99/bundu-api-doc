info:
  name: Get Transaction Pin Status
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/me/security/transaction-pin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: ""
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
            "success": true,
            "data": {
              "transactionPinSet": false,
              "passcodeSet": false,
              "passwordLockedOut": false,
              "passcodeLockedOut": false
            }
          }
