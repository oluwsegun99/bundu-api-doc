info:
  name: Get Auto Split Rules
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/me/auto-split-rules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
