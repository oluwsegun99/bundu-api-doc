info:
  name: Get Contribution Streak
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/me/contribution-streak"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: ""
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
            "success": true,
            "data": {
              "activeWeeks": 0,
              "label": "0 Weeks Flow"
            }
          }
