info:
  name: Get App Updates
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/me/app-updates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
