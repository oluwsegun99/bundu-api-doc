info:
  name: Get System Status
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/me/system-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
