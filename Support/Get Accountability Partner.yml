info:
  name: Get Accountability Partner
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/me/accountability-partner"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
